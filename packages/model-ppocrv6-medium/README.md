# PP-OCRv6 Medium model package

This private workspace placeholder reserves the exact dependency identity. The
release assembler creates the publishable pure-data package from the locked,
verified bundle in `models/generated`.
