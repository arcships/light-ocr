# Test-only PP-OCRv6 parity environment. Production artifacts never install these packages.
numpy==2.1.3
onnxruntime==1.22.0
paddlex[ocr-core]==3.7.0
pyclipper==1.3.0.post6
# click exposes this only on Windows; pin it explicitly so the cross-platform
# --require-hashes installation does not discover an unhashed dependency.
colorama==0.4.6
# humanfriendly and pandas also expose these only on Windows. Keep every
# platform-specific transitive dependency inside the same reviewed lock.
pyreadline3==3.5.6
tzdata==2026.3
